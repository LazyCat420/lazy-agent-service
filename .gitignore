# Dependency directories
node_modules/
jspm_packages/

# Build outputs
dist/
build/

# Debug logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
*.log

# Environment variables / secrets
.env
.env.production
.env.development
.env.local
.env.staging

# OS Metadata
.DS_Store
Thumbs.db

# Python virtual environment
.venv/
venv/
__pycache__/
*.pyc
/*.py
.pytest_cache/
.deepeval/

# Retired 2026-07-27: ./python was a mirror of trading-service/app + lazycat-sdk
# that the runtime image never contained (there is no interpreter in node:22-slim).
# Ignored rather than merely deleted so a stray local copy can never be re-committed.
/python/

# Local Secrets File
projects.json

# Temporary/Backup configs
*_temp.json
*_backup.json

